{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "extract": "deno run --allow-read --allow-env extract-functions.ts",
    "test": "deno test --allow-env",
    "check": "find . -name '*.ts' -not -path './samples/*' | xargs deno check"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "typescript": "npm:typescript@^5.0.0",
    "doctrine": "npm:doctrine@^3.0.0"
  }
}
